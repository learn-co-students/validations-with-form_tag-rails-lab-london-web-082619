
<% if @post.errors.full_messages.length > 0  %>
     <%= pluralize(@post.errors.full_messages.length "error") %> prohibited this post from being created

    <% @post.errors.full_messages.each do |message| %>
        <ul>
            <li> <%= message %> </li>
        </ul>
        
    <% end %>
<% end %>

<%= form_for(@post) do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>
    <%= f.label :category %>
    <%= f.text_field :category %>
    <%= f.label :content %>
    <%= f.text_area :content %>

    <%= f.submit %>

<% end %>